# === Credenciales y secretos (NUNCA subir) ===
.env
.env.*
!.env.example
*.key
*.pem
*-adminsdk-*.json
serviceAccountKey.json
secrets.json
# Service account de la integración Firebase (read-only): jamás en el repo
*service-account*.json
*serviceaccount*.json
firebase-service-account*.json

# === Netlify ===
.netlify/

# === Dependencias (si en el futuro se agrega Node) ===
node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml

# === Build / cache ===
dist/
build/
.cache/
.vite/

# === Logs ===
*.log
npm-debug.log*
yarn-debug.log*

# === Editores / OS ===
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
.DS_Store
Thumbs.db

# === Claude Code (config local del entorno) ===
.claude/settings.local.json

# === Temporales ===
*.tmp
scratchpad/
deno.lock
